[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gppm-ai-service"
version = "0.0.0"
description = "AI service for GPPM (local editable package)"
authors = [{name = "GPPM"}]

[tool.setuptools.packages.find]
where = ["."]
